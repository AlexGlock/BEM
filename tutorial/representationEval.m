function eval_grid = representationEval(w, grid)
%   REPRESENTATIONEVAL evaluates the representation formula for indirect
%   dirichlet BEM setting
nx = length(grid(1,:));
ny = length(grid(:,1));



eval_grid = zeros(ny,nx);

end

